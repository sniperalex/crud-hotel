# Exemplo de variáveis de ambiente (NUNCA coloque tokens reais aqui)
# Use este arquivo como referência e crie um `.env` local ou configure variáveis no provedor (Render)

# Se for usar Turso / libsql (remoto)
LIBSQL_URL=libsql://crud-sniperalex.aws-us-west-2.turso.io
LIBSQL_TOKEN=REPLACE_WITH_YOUR_TOKEN

# Ou para SQLite local (quando usar disco persistente)
SQLITE_PATH=/data/hotel_db.sqlite
